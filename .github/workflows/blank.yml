name: Sui Move Tests

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: mysten/sui-tools:ci
    steps:
      - uses: actions/checkout@v4
      - name: Change to atomic swap directory
        run: cd sui-fusion-plus/atomic_swap
      - name: Build
        run: cd sui-fusion-plus/atomic_swap && sui move build
      # - name: Run Test
      #   run: cd sui-fusion-plus/atomic_swap && sui move test
      #Enable when support --coverage flag in sui-tools docker
      # - name: coverage test
      #   run: sui move test --coverage
      # - name: Check test coverage
      #   run: echo "check test coveragerange"
